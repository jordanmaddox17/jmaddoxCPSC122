#include <iostream>
using namespace std;

#include "project6.h"


int main()
{
	bool bare;

 //Use of a static list 
 List lst;
 
 bare = lst.IsEmpty();
 if (bare == true)
 	cout << "lst: empty" << endl;
 else
 	cout << "lst: not empty" << endl;
 
 for (int i = 1; i < 5; i++)
   lst.PutItemH(i);
  lst.Print();
 	
 bare = lst.IsEmpty();
 if (bare == true)
 	cout << "lst: empty" << endl;
 else
 	cout << "lst: not empty" << endl;
 
 return 0;
}
